export default function draft(a) {
  return a + 2;
}

let count = 1;
if (count) {
  count += 1;
}

const a = [1, 1, 10, 15, 156, 145, 56];

const str = "eslint-config-prettier";

console.log(process.env.SECRET_KEY);
